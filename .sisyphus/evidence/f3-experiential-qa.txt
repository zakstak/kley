VERDICT: REJECT

Final-wave task: F3 Experiential QA for `agent-vm-baseline-update-lane`.
Date: 2026-04-03

Plan target:
- Execute the `saga-dev2` canary workflow end-to-end: build, apply, terminal/web smoke, then rollback once.

Commands executed:
- PASS — `bash tests/vm-baseline-check.sh`
- PASS — `git rev-parse HEAD` → `b746475d31c68726d4ba4d76042f41f3bb20008a`
- PASS — `nix build /home/zack/git/kley#nixosConfigurations.saga-dev2.config.system.build.toplevel`
- PASS — `nix build /home/zack/git/kley#nixosConfigurations.saga-dev.config.system.build.toplevel`
- PASS — `nix eval --raw /home/zack/git/kley#nixosConfigurations.saga-dev2.config.system.configurationRevision` → `b746475d31c68726d4ba4d76042f41f3bb20008a-dirty`
- PASS — `nix eval --raw /home/zack/git/kley#nixosConfigurations.saga-dev.config.system.configurationRevision` → `b746475d31c68726d4ba4d76042f41f3bb20008a-dirty`
- FAIL — `bash agent-vm/scripts/apply-local-checkout-canary.sh`
- FAIL — `bash agent-vm/scripts/validate-canary-kley.sh`
- FAIL — `bash agent-vm/scripts/recover-canary-after-failed-update.sh`

Observed behavior:
- Local repo-side regression checks passed: the developer-heavy manifest audit passed and the rollback/source-of-truth recovery markers are present in `agent-vm/README.md`.
- The canary apply wrapper executed the real Task 6 flow far enough to build `nixosConfigurations.saga-dev2.config.system.build.toplevel` and resolve `STORE_PATH=/nix/store/sw9jp00vylvc20scr7hli507kbwjq73b-nixos-system-saga-dev2-26.05.20260401.6201e20`.
- The first remote apply step failed exactly at SSH/DNS resolution:
  - `ssh: Could not resolve hostname saga-dev2: Name or service not known`
  - `error: writing to file: Broken pipe`
- The canary smoke wrapper failed at its first required SSH step with the same blocker while verifying `/home/agent/kley` on `agent@saga-dev2`:
  - `ssh: Could not resolve hostname saga-dev2: Name or service not known`
- The rollback wrapper failed at its first required SSH step with the same blocker while listing system generations on `agent@saga-dev2`:
  - `ssh: Could not resolve hostname saga-dev2: Name or service not known`
- Because no SSH session to `saga-dev2` could be established, no remote apply, no terminal smoke, no web smoke, and no runtime rollback actually executed on the canary VM in this environment.

Promotion decision:
- Promotion to `saga-dev` is NOT justified from actual executed checks.
- The local builds and metadata evaluation show the same checkout revision would be used for canary and baseline promotion, but the core experiential gate requires successful remote apply + smoke + rollback evidence on `saga-dev2`.

Conclusion:
- REJECT. This environment cannot complete the required experiential release gate because `agent@saga-dev2` is not resolvable, so the plan's required live canary execution was not achieved.
