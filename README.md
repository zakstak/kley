# kley

Run chat with `./docker-session.sh`; run self-improve with
`./docker-session.sh self-improve.sh`.

## Notes

- [Bindery UI port status](./BINDERY_UI_PORT_STATUS.md) — what is wired today
  vs. still UI-only, and why.
